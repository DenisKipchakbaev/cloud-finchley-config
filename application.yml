# temporary fix for bug: 
#   https://github.com/spring-cloud/spring-cloud-commons/issues/355 ,
#   https://github.com/spring-projects/spring-boot/issues/13042
spring.cloud.refresh.refreshable: false

#hystrix.command.default.execution.isolation.thread.timeoutInMilliseconds: 25000
ribbon:
  ConnectTimeout: 1000
  ReadTimeout: 2000

# Expose all actuator endpoints
management.endpoints.web.exposure.include: "*" # For production you should carefully choose which endpoints to expose
# management.endpoints.web.exposure.exclude: env,beans
management.endpoints.jmx.exposure.include: "*"

management.endpoint.health.showDetails: always #show details in actuator/health endpoint